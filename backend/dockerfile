FROM node:25

WORKDIR /app

COPY package*.json /app/
RUN npm install

CMD ["sh", "-c", "npx prisma generate && for i in $(seq 1 30); do npx prisma migrate deploy && break; echo \"Waiting for database... ($i/30)\"; sleep 2; done && npm run start"]